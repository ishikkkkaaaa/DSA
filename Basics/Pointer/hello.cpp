#include <iostream>
using namespace std;

int main()
{
    int a = 10;
    /* declaration of pointer */
    int *p;
    /* address of a, in pointer p */
    p = &a;
    cout << a;
    /* if c prog, printf("using pointer %d",*p) */
    return 0;
}
/* star is used in the time of declaration or referncing */